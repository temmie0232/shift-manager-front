export type Preset = {
    id: string;
    title: string;
    color: string;
    startTime: string;
    endTime: string;
};

export type PresetBackend = {
    id: string;
    title: string;
    color: string;
    start_time: string;
    end_time: string;
};